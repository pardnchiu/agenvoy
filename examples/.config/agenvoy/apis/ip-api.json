{
  "name": "ip_location",
  "description": "查詢指定 IP 的地理位置資訊，包含國家、城市、ISP、時區、經緯度等。留空則查詢本機 IP",
  "endpoint": {
    "url": "http://ip-api.com/json/{ip}",
    "method": "GET",
    "content_type": "json"
  },
  "parameters": {
    "ip": {
      "type": "string",
      "description": "要查詢的 IP 位址（IPv4 或 IPv6）。留空則查詢本機 IP",
      "required": false,
      "default": ""
    }
  },
  "response": {
    "format": "json"
  }
}
